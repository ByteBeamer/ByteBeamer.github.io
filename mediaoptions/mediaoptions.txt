<html>
<head>
<title>Media Player</title>
<link rel="stylesheet" href="https://bytebeamer.github.io/bookmarklet_res/spotify-options.css">
</head>
<body><label for="videos">Choose a video:</label>
<select id="videos">
</select>
<button id="pause">Play/Pause</button><br>
<button id="rewind">Rewind</button><button id="forward">Forward</button>
<iframe id="player" width=100% height=100% allow="autoplay; encrypted-media" frameborder="0"></iframe>
<script>
var frame = document.getElementById("player");
var select = document.getElementById("videos");
var songIds = ["jsDGb3if9bw", "nujn6wbr-e8", "fJkKvZH6eog", "Q_e5ZoOGWVk", "EDjYDfRunUk","Efa6BAWPm9o","S85ceVI4mfg","G5xSLbYMr-I","0R5uXlT54hs","jMqJf49DVc8","mBbPHYsPVQc"];
var songNames = ["雨とペトラ", "As it was", "アンドロイドガール", "Otome Dissection", "Otome Dissection (TeddyLoid Alllies)", "Losing My Religion","Beautiful Day","Sunroof","Outsider","The Vampire","Superstar"];
var currentIndex = 0;
var player = document.getElementById("player");

for (var i = 0; i < songIds.length; i++) {
  var elem = document.createElement("option");
  elem.value = songIds[i];
  elem.innerText = songNames[i];
  select.appendChild(elem);
}

select.onchange = function () {
  player.src = "https://youtube.com/embed/" + select.value + "?autoplay=1&enablejsapi=1";
}

var pauseBtn = document.getElementById("pause");
var forwardBtn = document.getElementById("forward");
var rewindBtn = document.getElementById("paue");

pauseBtn.onclick = pause();
fowardBtn.onclick = forward();
rewind.onclick = rewind();

function pause() {
  var e = new Event('keydown');
  e.which = e.keyCode = 32; // 32 is the keycode for the space bar
  frame.contentDocument.dispatchEvent(e);
}

function rewind() {
  var e = new Event('keydown');
  e.which = e.keyCode = 37; // 32 is the keycode for the space bar
  frame.contentDocument.dispatchEvent(e);
}

function forward() {
  var e = new Event('keydown');
  e.which = e.keyCode = 39; // 32 is the keycode for the space bar
  frame.contentDocument.dispatchEvent(e);
}
</script>
</body></html>
